# Epstein Archive - Environment Configuration Template
# Copy this file to .env.local and fill in your actual values
# NEVER commit .env.local to git!

# =============================================================================
# OpenRouter API Configuration (REQUIRED)
# =============================================================================
# Get your API key from: https://openrouter.ai/keys
# IMPORTANT: Keep this secret! Rotate immediately if exposed.
# Used for:
#   - Document summarization (via OPENROUTER_MODEL below)
#   - Entity type classification (Claude Haiku - fast, cheap, accurate)
#     Cost: ~$0.25/1M input tokens, ~$1.25/1M output tokens
OPENROUTER_API_KEY=your_openrouter_api_key_here

# OpenRouter Model Selection for Document Summarization (OPTIONAL)
# Default: openai/gpt-4o
# See available models: https://openrouter.ai/models
# Note: Entity classification always uses anthropic/claude-3-haiku
OPENROUTER_MODEL=openai/gpt-4o

# =============================================================================
# Archive.org Credentials (OPTIONAL)
# =============================================================================
# Only needed if using archive.org integration
# ARCHIVE_USERNAME=your_archive_username
# ARCHIVE_PASSWORD=your_archive_password

# =============================================================================
# Development Settings (OPTIONAL)
# =============================================================================
# Enable hot reload for development (default: true)
ENABLE_HOT_RELOAD=true

# Enable Pydantic validation (default: false)
USE_PYDANTIC=false

# Enable LLM entity classification (default: true)
# Requires OPENROUTER_API_KEY to be set
# Uses Claude Haiku via OpenRouter for fast, accurate classification
ENABLE_LLM_CLASSIFICATION=true

# Enable NLP entity classification fallback (default: true)
# Requires spaCy model: python -m spacy download en_core_web_sm
ENABLE_NLP_CLASSIFICATION=true

# =============================================================================
# SECURITY NOTES
# =============================================================================
# 1. This file is a TEMPLATE - it contains no real secrets
# 2. Never commit files containing real API keys or passwords
# 3. Rotate API keys immediately if they are ever exposed
# 4. Use environment variables or secret managers in production
# 5. See docs/deployment/SECURITY.md for production secret management
