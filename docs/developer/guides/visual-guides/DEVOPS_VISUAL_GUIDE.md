# DevOps Scripts Visual Guide

**Quick Summary**: Step-by-step guide and instructions for developers or users.

**Category**: Guide
**Status**: Active
**Last Updated**: 2025-11-24

**Key Points**:
- 🗺️ Script Ecosystem Map
- 🔄 Development Lifecycle

---

**Visual reference for Epstein Archive development operations workflow**

## 🗺️ Script Ecosystem Map

```
┌─────────────────────────────────────────────────────────────┐
│                    EPSTEIN ARCHIVE DEVOPS                   │
│                   Development & Deployment                   │
└─────────────────────────────────────────────────────────────┘
                              │
        ┌─────────────────────┼─────────────────────┐
        │                     │                     │
        ▼                     ▼                     ▼
  ┌──────────┐          ┌──────────┐          ┌──────────┐
  │   DEV    │          │ MONITOR  │          │  DEPLOY  │
  │  CYCLE   │          │   OPS    │          │  CYCLE   │
  └──────────┘          └──────────┘          └──────────┘
        │                     │                     │
        ├─ dev-start.sh       ├─ dev-status.sh     ├─ deploy.sh
        ├─ dev-stop.sh        ├─ dev-logs.sh       └─ health-check.sh
        └─ dev-logs.sh        └─ health-check.sh
```

## 🔄 Development Lifecycle

```
┌─────────────────────────────────────────────────────────────┐
│  DEVELOPMENT LIFECYCLE                                       │
└─────────────────────────────────────────────────────────────┘

1. START
   │
   ├─> ./scripts/dev-start.sh
   │   │
   │   ├─ Check prerequisites
   │   ├─ Check ports (8000, 5173)
   │   ├─ Start backend (FastAPI)
   │   ├─ Start frontend (Vite)
   │   ├─ Health checks
   │   └─ Monitor processes
   │
   ▼

2. DEVELOP
   │
   ├─> ./scripts/dev-status.sh
   │   └─ Monitor health
   │
   ├─> ./scripts/dev-logs.sh
   │   └─ View real-time logs
   │
   └─> Make code changes (auto-reload)
   │
   ▼

3. STOP
   │
   └─> ./scripts/dev-stop.sh
       │
       ├─ Graceful shutdown (SIGTERM)
       ├─ Force kill if needed (SIGKILL)
       ├─ Verify ports freed
       └─ Cleanup PID files
```

## 🚀 Deployment Pipeline

```
┌─────────────────────────────────────────────────────────────┐
│  DEPLOYMENT PIPELINE                                         │
└─────────────────────────────────────────────────────────────┘

./scripts/deploy.sh --env {staging|production}
│
├─ 1. STOP DEV SERVERS
│  └─> ./scripts/dev-stop.sh
│
├─ 2. RUN TESTS
│  ├─ Python tests (pytest)
│  ├─ Frontend tests (npm test)
│  └─ API smoke tests
│
├─ 3. BUILD FRONTEND
│  └─ npm run build → dist/
│
├─ 4. BACKUP CURRENT
│  └─ backups/backup-{timestamp}-{service}/
│
├─ 5. CREATE PACKAGE
│  └─ deploy/epstein-archive-{env}-{timestamp}.tar.gz
│
├─ 6. DEPLOY
│  ├─ Extract package
│  ├─ Copy frontend dist/
│  └─ Update backend
│
├─ 7. VERIFY
│  └─> ./scripts/health-check.sh
│
└─ 8. ROLLBACK (if failure)
   └─> ./scripts/deploy.sh --rollback
```

## 📊 Monitoring Dashboard

```
┌─────────────────────────────────────────────────────────────┐
│  STATUS MONITORING                                           │
└─────────────────────────────────────────────────────────────┘

./scripts/dev-status.sh
│
├─ BACKEND (Port 8000)
│  ├─ Status:  ✅ Running / ❌ Stopped
│  ├─ PID:     12345
│  ├─ Uptime:  2h 15m
│  ├─ Memory:  245 MB
│  ├─ Health:  ✅ OK / ❌ Error
│  └─ Logs:    logs/backend.log
│
├─ FRONTEND (Port 5173)
│  ├─ Status:  ✅ Running / ❌ Stopped
│  ├─ PID:     12346
│  ├─ Uptime:  2h 15m
│  ├─ Memory:  128 MB
│  ├─ Health:  ✅ OK / ❌ Error
│  └─ Logs:    logs/frontend.log
│
└─ RECENT LOGS
   ├─ Backend:  [last 5 lines]
   └─ Frontend: [last 5 lines]
```

## 🔍 Health Check Flow

```
┌─────────────────────────────────────────────────────────────┐
│  HEALTH CHECK SEQUENCE                                       │
└─────────────────────────────────────────────────────────────┘

./scripts/health-check.sh
│
├─ 1. CHECK BACKEND
│  │
│  ├─ GET http://localhost:8000/api/rag/stats
│  │  ├─ 200 OK → ✅ Healthy
│  │  └─ Error  → ❌ Down
│  │
│  └─ Timeout: 2 seconds
│
├─ 2. CHECK FRONTEND
│  │
│  ├─ GET http://localhost:5173/
│  │  ├─ 200 OK → ✅ Healthy
│  │  └─ Error  → ❌ Down
│  │
│  └─ Timeout: 2 seconds
│
├─ 3. CHECK VECTOR STORE
│  │
│  ├─ GET http://localhost:8000/api/news/stats
│  │  ├─ 200 OK → ✅ Accessible
│  │  └─ Error  → ⚠️  Warning (non-critical)
│  │
│  └─ Timeout: 2 seconds
│
└─ EXIT CODE
   ├─ 0 → All healthy
   ├─ 1 → Critical failure
   └─ 2 → Non-critical warning
```

## 📝 Log Viewing Workflow

```
┌─────────────────────────────────────────────────────────────┐
│  LOG VIEWING & FILTERING                                     │
└─────────────────────────────────────────────────────────────┘

./scripts/dev-logs.sh
│
├─ FOLLOW MODE (default)
│  │
│  ├─ Backend logs  → [BACKEND] (blue)
│  ├─ Frontend logs → [FRONTEND] (green)
│  └─ Real-time tail -f
│
├─ FILTERING OPTIONS
│  │
│  ├─ --backend-only     → Backend only
│  ├─ --frontend-only    → Frontend only
│  ├─ --errors-only      → Errors/warnings only
│  ├─ --grep "keyword"   → Search pattern
│  └─ --last 50          → Last 50 lines
│
└─ COLOR CODING
   ├─ 🔵 [BACKEND]  → Blue prefix
   ├─ 🟢 [FRONTEND] → Green prefix
   ├─ 🔴 ERROR      → Red text
   ├─ 🟡 WARNING    → Yellow text
   └─ 🔵 INFO       → Cyan text
```

## 🎯 Port Management

```
┌─────────────────────────────────────────────────────────────┐
│  PORT CONFLICT RESOLUTION                                    │
└─────────────────────────────────────────────────────────────┘

./scripts/dev-start.sh
│
├─ Check Port 8000 (Backend)
│  │
│  ├─ Port Free? ─────────────> Start Backend
│  │
│  └─ Port In Use?
│     │
│     ├─ Identify Process (PID)
│     ├─ Prompt User: "Kill process? (y/n)"
│     │
│     ├─ YES → Kill Process → Start Backend
│     └─ NO  → Exit (Error)
│
└─ Check Port 5173 (Frontend)
   │
   ├─ Port Free? ─────────────> Start Frontend
   │
   └─ Port In Use? → [Same flow as above]
```

## 🗂️ File Tracking System

```
┌─────────────────────────────────────────────────────────────┐
│  PID & LOG FILE STRUCTURE                                    │
└─────────────────────────────────────────────────────────────┘

PROJECT_ROOT/
│
├─ .dev-pids              # JSON with both PIDs
│  {
│    "backend": {
│      "pid": 12345,
│      "port": 8000,
│      "started_at": "2025-11-20T11:30:00",
│      "log_file": "logs/backend.log"
│    },
│    "frontend": {
│      "pid": 12346,
│      "port": 5173,
│      "started_at": "2025-11-20T11:30:00",
│      "log_file": "logs/frontend.log"
│    }
│  }
│
├─ .backend.pid           # Backend process ID
├─ .frontend.pid          # Frontend process ID
│
├─ logs/
│  ├─ backend.log         # Backend logs (append)
│  └─ frontend.log        # Frontend logs (append)
│
├─ deploy/
│  └─ epstein-archive-{env}-{timestamp}.tar.gz
│
└─ backups/
   ├─ backup-{timestamp}-backend/
   └─ backup-{timestamp}-frontend/
```

## 🔄 Process Monitoring Loop

```
┌─────────────────────────────────────────────────────────────┐
│  CONTINUOUS PROCESS MONITORING                               │
└─────────────────────────────────────────────────────────────┘

./scripts/dev-start.sh (main loop)
│
└─> while true; do
    │
    ├─ Sleep 5 seconds
    │
    ├─ Check Backend Process
    │  │
    │  ├─ Running? → Continue
    │  └─ Dead?    → ❌ Alert & Cleanup
    │
    ├─ Check Frontend Process
    │  │
    │  ├─ Running? → Continue
    │  └─ Dead?    → ❌ Alert & Cleanup
    │
    └─ Ctrl+C? → Graceful Shutdown
       │
       ├─ Kill Backend (SIGTERM)
       ├─ Kill Frontend (SIGTERM)
       ├─ Wait 2 seconds
       ├─ Force kill if needed (SIGKILL)
       └─ Exit
    done
```

## 🎨 Color Coding System

```
┌─────────────────────────────────────────────────────────────┐
│  VISUAL STATUS INDICATORS                                    │
└─────────────────────────────────────────────────────────────┘

🔵 INFO     │ General information
────────────┼───────────────────────────────────
✅ SUCCESS  │ Operation successful
────────────┼───────────────────────────────────
⚠️  WARNING │ Non-critical issue
────────────┼───────────────────────────────────
❌ ERROR    │ Critical failure

┌─────────────────────────────────────┐
│  TEXT COLORS                        │
├─────────────────────────────────────┤
│  🔵 BLUE    → Info, process status  │
│  🟢 GREEN   → Success, healthy      │
│  🟡 YELLOW  → Warning, partial fail │
│  🔴 RED     → Error, stopped        │
│  🔵 CYAN    → Log messages          │
└─────────────────────────────────────┘
```

## 🏃 Quick Action Matrix

```
┌─────────────────────────────────────────────────────────────┐
│  SCENARIO → ACTION                                           │
└─────────────────────────────────────────────────────────────┘

┌──────────────────────────┬────────────────────────────────┐
│ SCENARIO                 │ COMMAND                        │
├──────────────────────────┼────────────────────────────────┤
│ Start development        │ ./scripts/dev-start.sh         │
│ Stop development         │ ./scripts/dev-stop.sh          │
│ Check if running         │ ./scripts/dev-status.sh        │
│ View logs                │ ./scripts/dev-logs.sh          │
│ Quick health check       │ ./scripts/health-check.sh      │
│ Port already in use      │ ./scripts/dev-stop.sh --force  │
│ Backend crashed          │ ./scripts/dev-logs.sh --errors │
│ Deploy to staging        │ ./scripts/deploy.sh --env stag │
│ Rollback deployment      │ ./scripts/deploy.sh --rollback │
│ Clean restart            │ ./scripts/dev-start.sh --restr │
└──────────────────────────┴────────────────────────────────┘
```

## 🚦 Status States

```
┌─────────────────────────────────────────────────────────────┐
│  SERVICE STATUS STATES                                       │
└─────────────────────────────────────────────────────────────┘

   ┌─────────┐
   │ STOPPED │ ─────> ./scripts/dev-start.sh
   └─────────┘              │
        ▲                   │
        │                   ▼
        │            ┌────────────┐
        │            │ STARTING   │ (Health checks)
        │            └────────────┘
        │                   │
        │                   ▼
        │            ┌────────────┐
        │            │ RUNNING    │ ◄─── ./scripts/dev-status.sh
        │            │  ✅ Healthy │
        │            └────────────┘
        │                   │
        │                   │ Crash detected
        │                   ▼
        │            ┌────────────┐
        └────────────┤ UNHEALTHY  │ ──> ./scripts/dev-logs.sh
                     │  ❌ Error   │
                     └────────────┘
```

## 📦 Deployment Package Contents

```
┌─────────────────────────────────────────────────────────────┐
│  DEPLOYMENT PACKAGE STRUCTURE                                │
└─────────────────────────────────────────────────────────────┘

epstein-archive-{env}-{timestamp}.tar.gz
│
├─ frontend/              # Production build
│  └─ (dist/ contents)
│
├─ backend/               # Backend server
│  ├─ app.py
│  ├─ routes/
│  ├─ services/
│  └─ models/
│
├─ requirements.txt       # Python dependencies
│
├─ .env                   # Environment config
│
└─ MANIFEST.json          # Deployment metadata
   {
     "version": "20251120_113000",
     "environment": "staging",
     "created_at": "2025-11-20T11:30:00Z",
     "git_commit": "abc123...",
     "git_branch": "main"
   }
```

## 🔧 Environment Variables Flow

```
┌─────────────────────────────────────────────────────────────┐
│  ENVIRONMENT VARIABLE CONFIGURATION                          │
└─────────────────────────────────────────────────────────────┘

User Environment
│
├─ BACKEND_PORT=8000
├─ FRONTEND_PORT=5173
├─ LOG_LEVEL=INFO
├─ DEPLOY_TARGET=staging
└─ BACKUP_COUNT=3
   │
   ▼
Script Execution
│
├─ ./scripts/dev-start.sh
│  └─> Uses: BACKEND_PORT, FRONTEND_PORT, LOG_LEVEL
│
├─ ./scripts/deploy.sh
│  └─> Uses: DEPLOY_TARGET, BACKUP_COUNT
│
└─ ./scripts/health-check.sh
   └─> Uses: BACKEND_PORT, FRONTEND_PORT
```

---

**For detailed documentation**: `scripts/DEVOPS_README.md`
**Quick reference**: `DEVOPS_QUICK_REF.md`
**Last Updated**: 2025-11-20
